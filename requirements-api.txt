# =============================================================================
# Chonkie OSS API – Python dependencies
# =============================================================================
# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Chonkie with all extras needed to run every endpoint.
# semantic  – SemanticChunker (sentence-transformers / model2vec)
# code      – CodeChunker (tree-sitter)
# openai    – EmbeddingsRefinery with OpenAI models
chonkie[semantic,code,openai]>=1.0.0
